{
  "name": "Integration - Escalation Endpoint with Valid Data",
  "description": "Ensures that the /api/assistant/escalate endpoint triggers both notification and audit logging, and returns a success message.",
  "test_type": "integration",
  "input": "POST to /api/assistant/escalate with JSON body containing issue_details and user_context.",
  "output": "JSON response with success: true and escalation confirmation message.",
  "success_criteria": [
    "HTTP status code is 200",
    "response['success'] is True",
    "response['response'] contains 'escalated to human staff'"
  ],
  "error_scenarios": [
    "Missing issue_details in request",
    "Notification or audit logging service failure",
    "Malformed JSON input"
  ]
}