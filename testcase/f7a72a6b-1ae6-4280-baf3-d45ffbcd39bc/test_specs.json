{
  "name": "Functional - Successful Ticket Verification via API",
  "description": "Validates that the /api/assistant/verify_ticket endpoint correctly verifies a valid and unused ticket, logs the audit event, and returns the expected response.",
  "test_type": "functional",
  "input": "POST to /api/assistant/verify_ticket with valid ticket_code and event_id in JSON body.",
  "output": "JSON response indicating success, authorized=true, and a formatted success message.",
  "success_criteria": [
    "HTTP status is 200",
    "response['success'] is True",
    "response['authorized'] is True",
    "response['response'] contains 'is valid and unused'"
  ],
  "error_scenarios": [
    "Ticket code is invalid or already used",
    "Ticket database API is unreachable",
    "Malformed JSON input"
  ]
}