{
  "name": "Functional - Invalid Ticket Returns Failure Response",
  "description": "Checks that submitting an invalid or already used ticket to /api/assistant/verify_ticket returns authorized=false and the correct failure message.",
  "test_type": "functional",
  "input": "POST to /api/assistant/verify_ticket with a ticket_code/event_id pair that is invalid or marked as used.",
  "output": "JSON response with authorized=false and a failure message.",
  "success_criteria": [
    "HTTP status code is 200",
    "response.success is True",
    "response.authorized is False",
    "response.response contains 'invalid or already used'"
  ],
  "error_scenarios": [
    "Ticket database API unavailable",
    "Audit log API failure",
    "Malformed JSON input"
  ]
}