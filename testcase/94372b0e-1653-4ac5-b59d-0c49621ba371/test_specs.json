{
  "name": "Functional - Handle Help Intent in Message Endpoint",
  "description": "Checks that when a user sends a message containing 'help', the /api/assistant/message endpoint returns the help template response.",
  "test_type": "functional",
  "input": "POST to /api/assistant/message with user_input='help' and no user_context.",
  "output": "JSON response with a help message.",
  "success_criteria": [
    "HTTP status is 200",
    "response['success'] is True",
    "response['response'] contains 'If you need help'"
  ],
  "error_scenarios": [
    "Malformed JSON input"
  ]
}