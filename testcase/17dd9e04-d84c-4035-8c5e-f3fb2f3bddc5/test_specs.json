{
  "name": "Integration - LLM Endpoint with User Prompt",
  "description": "Tests that the /api/assistant/llm endpoint interacts with the LLM and returns a generated response.",
  "test_type": "integration",
  "input": "POST to /api/assistant/llm with JSON body containing user_prompt and optional context.",
  "output": "JSON response with success: true and a non-empty LLM-generated response.",
  "success_criteria": [
    "HTTP status code is 200",
    "response['success'] is True",
    "response['response'] is not empty"
  ],
  "error_scenarios": [
    "Missing user_prompt in request",
    "LLM API failure",
    "Malformed JSON input"
  ]
}