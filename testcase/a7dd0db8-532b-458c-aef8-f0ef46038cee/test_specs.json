{
  "name": "Integration - Message Endpoint with Unknown Intent Uses LLM",
  "description": "Ensures that when a user sends an unrecognized message, the /api/assistant/message endpoint routes to the LLM and returns a generated response.",
  "test_type": "integration",
  "input": "POST to /api/assistant/message with user_input='What time does the concert start?' and no ticket code present.",
  "output": "JSON response with a string generated by the LLM.",
  "success_criteria": [
    "HTTP status code is 200",
    "response.success is True",
    "response.response is a non-empty string"
  ],
  "error_scenarios": [
    "LLM API failure",
    "Malformed JSON input"
  ]
}