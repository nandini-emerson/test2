{
  "name": "Functional - Handle Help Intent in Message Endpoint",
  "description": "Checks that the /api/assistant/message endpoint returns the help template when the user input contains 'help'.",
  "test_type": "functional",
  "input": "POST to /api/assistant/message with user_input: 'help', user_context: null.",
  "output": "JSON response with a help message template.",
  "success_criteria": [
    "HTTP status code is 200",
    "response['success'] is True",
    "response['response'] contains 'If you need help'"
  ],
  "error_scenarios": [
    "Malformed JSON input"
  ]
}